@page "/files/{Id:int}/access"
@inject NavigationManager Nav

<PageTitle>Права доступа</PageTitle>

<AccessRightsDialog IsVisible="true" EntityType="File" EntityId="@Id" OnClose="Back" />

@code {
    [Parameter] public int Id { get; set; }

    void Back() => Nav.NavigateTo("/files");
}
